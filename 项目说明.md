设计并实现一个基于YOLOv8的多模式智能门禁系统，通过融合多种认证技术构建一个安全、便捷且智能化的出入口管理解决方案。该系统将整合四种主流的身份认证方式：基于改进YOLOv8算法的人脸识别、蓝牙设备认证、NFC近场通信识别以及二维码验证，以满足不同场景下的使用需求。

在技术实现层面，项目采用分层架构设计。软件系统以识别系统作为边缘计算节点，运行优化的YOLOv8人脸识别模型，负责复杂的图像处理和识别任务；同时基于Python+MySQL+HTML技术栈开发完整的后台管理系统，实现用户信息管理、权限分配、访客管理和日志审计等功能。硬件系统则以ESP32微控制器为核心执行单元，配合PN532 NFC读写模块、蓝牙模块和摄像头等外设，构建完整的门禁控制终端。



```
Stop-Process -Name python -Force -ErrorAction SilentlyContinue; Start-Sleep -Seconds 1; Set-Location U:\BYSJ\yolov-door\yolov8-door\SmartAccess; $p = Start-Process -FilePath U:\BYSJ\yolov8_face_v2\python.exe -ArgumentList '-m','uvicorn','app.main:app','--port','8000','--host','127.0.0.1' -WorkingDirectory 'U:\BYSJ\yolov-door\yolov8-door\SmartAccess' -PassThru -WindowStyle Hidden; Write-Host "应用已启动，PID: $($p.Id)"; Start-Sleep -Seconds 3; Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Select-Object @{Name='端口';Expression={$_.LocalPort}},@{Name='状态';Expression={$_.State}},@{Name='PID';Expression={$_.OwningProcess}}; try { $r = Invoke-WebRequest -Uri 'http://localhost:8000/health' -UseBasicParsing -TimeoutSec 5; $r.Content } catch { Write-Host '健康检查失败:' $_.Exception.Message }
```
